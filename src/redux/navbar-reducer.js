const initialState = {
    friendsData: [
        {id:1, name: "Svetlana"},
        {id:2, name: "Mike"},
        {id:3, name: "Sofia"},
    ]
}

export const navbarReducer = (state = initialState, action) => {

    return state;
}